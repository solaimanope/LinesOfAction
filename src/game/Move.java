package game;

public class Move {
    public final Cell source, destination;
    Move(Cell source, Cell destination) {
        this.source = source;
        this.destination = destination;
    }
}
